plugins {
    id 'nf-prov@1.2.3'
}

params {
    outdir = 'results'
}

prov {
    formats {
        quilt {
            file = "${params.outdir}/manifest.json"
            overwrite = true
        }
    }
}
